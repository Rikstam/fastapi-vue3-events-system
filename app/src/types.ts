export interface EventItem {
    id?: number;
    category: string;
    title: string;
    description: string;
    location: string;
    date: string;
    time: string;
    organization: string;
}

export interface UserItem {
    user: string;
}